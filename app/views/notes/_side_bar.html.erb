
<div class="create-folder">
    <%= link_to "➕新規フォルダを作成", new_folder_path %>
</div>

<div class="side-contents">
  <% current_user.folders.each do |folder| %>
    <div class="folder">
      <div class="folder-name">
        <%= link_to folder.name, folder_notes_path(folder) %></a>
      </div>
    </div>
  <% end %>
</div>